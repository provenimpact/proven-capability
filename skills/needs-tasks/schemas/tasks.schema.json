{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://provenimpact.github.io/proven-needs/schemas/tasks.schema.json",
  "title": "Implementation Tasks",
  "description": "Task graph for feature implementation. Tasks are nodes in a DAG with explicit depends_on edges. Full traceability back to spec.yaml requirements.",
  "version": "2.0.0",
  "type": "object",
  "required": ["schema_version", "feature", "version", "status", "source_spec_version", "last_updated", "overview", "tasks"],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Version of the tasks schema this file was written against."
    },
    "feature": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Feature slug matching the feature package directory name."
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "SemVer version of this task list."
    },
    "status": {
      "type": "string",
      "enum": ["Current", "Stale", "Implemented"],
      "description": "Current = aligned with design. Stale = design changed. Implemented = all tasks done."
    },
    "source_design_version": {
      "type": "string",
      "description": "Version of design.adoc used to derive tasks. 'n/a' if design was skipped."
    },
    "source_spec_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Version of spec.yaml used to derive tasks."
    },
    "last_updated": {
      "type": "string",
      "format": "date",
      "description": "Date of last update in YYYY-MM-DD format."
    },
    "overview": {
      "type": "string",
      "minLength": 1,
      "description": "Brief summary of what is being implemented, total tasks."
    },
    "tasks": {
      "type": "array",
      "minItems": 1,
      "description": "All tasks as a flat list. Dependencies are explicit via depends_on.",
      "items": {
        "$ref": "#/$defs/task"
      }
    }
  },
  "$defs": {
    "task": {
      "type": "object",
      "required": ["id", "title", "stories", "requirements", "description"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^TASK-\\d{3}$",
          "description": "Unique task ID (e.g., 'TASK-001'). Sequential across the entire file."
        },
        "title": {
          "type": "string",
          "minLength": 1,
          "description": "Clear, actionable task title."
        },
        "done": {
          "type": "boolean",
          "default": false,
          "description": "Whether this task has been completed."
        },
        "depends_on": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^TASK-\\d{3}$"
          },
          "description": "Task IDs that must complete before this task can start. Empty means no dependencies (root task)."
        },
        "components": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Design components involved. Omit when no design exists (source_design_version is 'n/a')."
        },
        "stories": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^US-\\d{3}$"
          },
          "minItems": 1,
          "description": "Story IDs this task contributes to."
        },
        "requirements": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^[A-Z]{2,5}-\\d{3}$"
          },
          "minItems": 1,
          "description": "Requirement IDs this task satisfies."
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "What to implement and key details."
        }
      }
    }
  }
}